name: flutter_app_mensuales
description: Pagos de intereses mensuales

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.3
  http: ^0.11.3+16
  shared_preferences: any
  connectivity: ^0.4.2
  datetime_picker_formfield: ^2.0.0
  fluttertoast: ^3.0.3
  flutter_screenutil: ^0.4.2
  badges: ^1.1.1
  giffy_dialog: ^1.7.0
  validators: ^2.0.0+1
  dropdown_formfield: ^0.1.2
  cached_network_image:
  intent:
  url_launcher: ^5.0.3
  get_it: ^1.0.3+2
  permission_handler: ^3.2.2
  progress_dialog: ^1.2.2
  simple_animations: ^1.3.3
  global_configuration:
  flutter_svg: ^0.19.3
  sizer: ^1.1.8
  smooth_star_rating: 1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_native_splash: ^0.1.9

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspe

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  assets:
         - assets/
         - assets/images/6409.jpg
         - assets/images/project_empty.png
         - assets/images/background.png
         - assets/images/light-1.png
         - assets/images/light-2.png
         - assets/images/clock.png
         - assets/images/silueta.png
         - assets/image_01.png
         - assets/image_02.png
         - assets/logo.png
         - assets/login_fondo_01.png
         - assets/login_fondo_02.png
         - assets/login_fondo_3.png
         - assets/login_fondo_04.png
         - assets/login_fondo_05.png
         - assets/login_fondo_06.png
         - assets/login_fondo_07.png
         - assets/avatar_hombre.jpg
         - assets/avatar_mujer.jpg
         - assets/images/personal_profile.png
         - assets/images/personal_profile2.png
         - assets/images/grupo_clientes.png
         - assets/images/pressta_splash.png
         - assets/images/delete_1.svg


  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:

  fonts:
      - family: CustomIcons
        fonts:
          - asset: assets/CustomIcons.ttf

      - family: Poppins-Bold
        fonts:
          - asset: assets/Poppins-Bold.ttf

      - family: Poppins-Medium
        fonts:
          - asset: assets/Poppins-Medium.ttf
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
